{
  "workerUrl": "",
  "enrollMachineId": "",
  "secretName": "",
  "heartbeatIntervalMs": 60000,
  "gitSyncIntervalMs": 300000,
  "sessionScanIntervalMs": 120000,
  "daemonPort": 4269,
  "maxBackoffMs": 300000,
  "_comment": "workerUrl, enrollMachineId (slug for npm run enroll issue; default: this hostname), secretName (KV name; default pi-secrets-<machineId>). Intervals in ms."
}
